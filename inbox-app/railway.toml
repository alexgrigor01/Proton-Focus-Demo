# When the Git repo root is the monorepo (parent folder), use `/railway.toml` at repo root instead.
# This file applies only if Railway Root Directory is set to `inbox-app`.
[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start"
